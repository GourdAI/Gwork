# ── 构建产物 ────────────────────────────────────────────────────────────────

# Rust 编译输出（release 单次约数 GB）
src-tauri/target/

# tauri 构建期生成的 ACL / schema。删掉后 cargo check 会自动重建，已实测。
src-tauri/gen/schemas/

# build.rs 按 #[tauri::command] 清单自动生成的权限定义（14 个 toml）。
# 同样实测过：删除后 cargo check 会全量重建。手写权限应放在
# permissions/ 根目录下，不会被这条规则波及。
src-tauri/permissions/autogenerated/

# configure-updater.js 的生成物：每次构建按当前环境重新生成。
# 入库会把「上次构建的降级状态」带到下次构建。
src-tauri/updater.generated.conf.json

# prepare-ui.js 从 gourd-ai-agent/src/main/resources/static 全量拷贝的前端资源。
# 该脚本每次运行都会先清空本目录再覆盖，入库只会制造无意义的巨量 diff。
# 唯一源在 gourd-ai-agent，改前端请改那边。
/ui/

# cmd/sync-backend.js 同步的后端产物：jar 来自 gourd-ai-agent/target，jre 由 jlink 生成，
# build-manifest.json 由 gen-build-manifest.js 生成。三者均为构建产物，且 jar+jre 合计
# 近 100MB，绝不入库（与 gourd-ai-desktop/.gitignore 的同类规则保持一致）。
/build/

# ── 签名密钥（泄露即失去更新链的信任根，绝不可入库）──────────────────────────
# 私钥只应存在于 ~/.tauri/ 与 CI Secret；公钥入库的正确位置是
# src-tauri/tauri.conf.json 的 plugins.updater.pubkey，而非独立文件。
*.key
*.pem
!src-tauri/**/*.pub

# 更新签名产物：随 bundle 生成，发布时上传，不入库
*.sig

# ── Node ───────────────────────────────────────────────────────────────────
node_modules/
package-lock.json

# ── OS ─────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
